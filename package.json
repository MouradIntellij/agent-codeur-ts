{
  "name": "agent-codeur-ts",
  "version": "1.0.0",
  "description": "Agent IA de codage pédagogique en TypeScript (Ollama local) - version full-stack",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "web": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
